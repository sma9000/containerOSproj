apiVersion: v1
kind: Service
metadata:
  name: webmvc
  labels:
    app.kubernetes.io/name: webmvc
    app.kubernetes.io/part-of: eshop
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: webmvc
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: grpc
      port: 81
      targetPort: 81
